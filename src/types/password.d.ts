declare namespace HttpPayload{
    type pwd={
        user_name:string,oldPwd: string, newPwd: string, newPwd2: string
    }
}
declare namespace Vm {
    type pwd = {
        user_name: string, oldPwd: string, newPwd: string,newPwd2:string
    }
}